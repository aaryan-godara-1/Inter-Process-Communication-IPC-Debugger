"""
system_monitor/ — Real OS-level process and IPC monitoring.

Uses psutil to collect live kernel data: process list, CPU/memory metrics,
open file handles, and network sockets to detect real IPC connections.
"""
